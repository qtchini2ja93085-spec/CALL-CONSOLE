{
  "name": "callconsole",
  "version": "1.0.0",
  "private": true,
  "description": "CALLCONSOLE offline Android app",
  "scripts": {
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0"
  }
}
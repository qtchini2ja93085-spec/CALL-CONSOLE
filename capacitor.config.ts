import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.epmsolutions.callconsole',
  appName: 'CALLCONSOLE',
  webDir: 'www'
};

export default config;

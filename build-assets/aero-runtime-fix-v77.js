/* CALLCONSOLE AERO v77.1 runtime hardening: race/cancellation guard + mode-button unlock */
(function(){'use strict';var AI_ENDPOINT='https://intelligent-outreach-builder.vercel.app/api/callconsole-ai-fast',activeAI=null;function unlock(){var box=document.getElementById('aero-script-modes');if(!box)return;box.classList.add('aero-runtime-ready');box.querySelectorAll('button[data-mode]').forEach(function(b){b.disabled=false;b.removeAttribute('disabled');b.removeAttribute('aria-disabled')})}function abortActiveAI(){if(activeAI){try{activeAI.abort()}catch(_){}activeAI=null}}var originalFetch=window.fetch.bind(window);window.fetch=function(input,init){var url=typeof input==='string'?input:(input&&input.url)||'',method=((init&&init.method)||(input&&input.method)||'GET').toUpperCase();if(method==='POST'&&url===AI_ENDPOINT){abortActiveAI();var controller=new AbortController();activeAI=controller;var callerSignal=(init&&init.signal)||(input&&input.signal);if(callerSignal){if(callerSignal.aborted)controller.abort();else callerSignal.addEventListener('abort',function(){try{controller.abort()}catch(_){}},{once:true})}var next=Object.assign({},init||{},{signal:controller.signal});return originalFetch(input,next).finally(function(){if(activeAI===controller)activeAI=null})}return originalFetch(input,init)};function bind(){var box=document.getElementById('aero-script-modes');if(!box||box.dataset.aeroHardeningBound==='1')return;box.dataset.aeroHardeningBound='1';box.addEventListener('click',function(ev){var b=ev.target&&ev.target.closest?ev.target.closest('button[data-mode]'):null;if(b&&b.dataset.mode!=='ai')abortActiveAI()},true)}function tick(){unlock();bind()}tick();new MutationObserver(tick).observe(document.documentElement,{childList:true,subtree:true});setInterval(tick,750)})();

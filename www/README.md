Place the existing, tested CALLCONSOLE web application files in this directory.

Required minimum entry point:
- index.html

Do not replace or simplify the existing script-tailoring logic when packaging the app for Android.
